#include <stdio.h>

/**
* main - Script that prints the alphabet backwards
* Return: 0 (Success)
*/

int main(void)
{
	int tebahpla;

	for (tebahpla = 'z'; tebahpla >= 'a'; tebahpla--)
	putchar(tebahpla);

	putchar('\n');
	return (0);
}
