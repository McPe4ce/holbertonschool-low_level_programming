#include "main.h"

/**
* _abs - Prints the absolute value of an int
* @int: Integer
* Return: n (Positive)
*/

int _abs(int)
{
	int AbsolX

	if (AbsolX < 0)
	{
		return (-AbsolX);
	}
	return (AbsolX);
}
